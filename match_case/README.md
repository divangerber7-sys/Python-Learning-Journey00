## Lesson 38 – Match-Case (Switch) 🔀
📌 *Concepts learned:*  
- Alternative to long `if-elif` chains.  
- `match` is followed by multiple `case` blocks.  
- `case _:` acts like a `default` / wildcard.  
- Supports multiple matches using the `|` operator.  
